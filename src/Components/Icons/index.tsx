import {
    Visibility,
    VisibilityOff,
    Lock,
    Search,
    SystemUpdateAlt,
    Notifications,
    FilterList,
} from "@mui/icons-material";
import { styled } from "@mui/material";

export const PasswordOnIcon = styled(Visibility)(() => ({}));

export const PasswordOffIcon = styled(VisibilityOff)(() => ({}));

export const LockIcon = styled(Lock)(() => ({}));

export const SearchIcon = styled(Search)(() => ({}));

export const ExportIcon = styled(SystemUpdateAlt)(() => ({}));

export const NotificationIcon = styled(Notifications)(() => ({}));

export const FilterListIcon = styled(FilterList)(() => ({}));
